Each line represents a user sequential hashtag adoption activity in an hour.

For each line, the format is:
time;userid;hashtag id,hashtag id...;hashtag id,hashtag id ...;  

In each line, hashtags from different tweets are separated by a comma, hashtags within one tweet are separated by a semicolon.
